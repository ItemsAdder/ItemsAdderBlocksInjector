package com.volmit.iris.util.data;

import org.bukkit.block.data.BlockData;

public class B {

    /**
     * This is just because Iris doesn't have a public maven repo :(
     * It is just simpler like that.
     * <p>
     * It is excluded from the build so it doesn't cause any issues.
     * <p>
     * see: <a href="https://github.com/VolmitSoftware/Iris/blob/master/core/src/main/java/com/volmit/iris/util/data/B.java#L702"></a>
     *
     * @param ignoredNamespace namespace
     * @param ignoredKey key
     * @param ignoredBlockData blockData
     */
    public static void registerCustomBlockData(String ignoredNamespace, String ignoredKey, BlockData ignoredBlockData) {
    }

}
